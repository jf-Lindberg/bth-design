{% include 'incl/header.twig' %}

<body>
	{% include 'incl/nav.twig' %}
	<div class="tech-container">
		<div class="tech-nav">
			<ul class="nav-list">
                <h3>Navigation</h3>
				{% for page in pages("technology")|reverse if not page.hidden %}
					<li class="nav-item">
						<a href="{{ page.url }}">{{ page.title }}</a>
					</li>
				{% endfor %}
			</ul>
		</div>

		<div class="main technology" role="main">
			{{ content }}
		</div>
	</div>

	{% include 'incl/footer.twig' %}</body></body></html>
